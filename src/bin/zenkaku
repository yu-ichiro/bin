#!/usr/bin/env python
import sys

HAN = chr(0x20) + "".join(chr(0x21 + i) for i in range(94))
ZEN = chr(0x3000) + "".join(chr(0xff01 + i) for i in range(94))

table = str.maketrans(HAN, ZEN)

for line in sys.stdin:
    sys.stdout.write(line.translate(table))
